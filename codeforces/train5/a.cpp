#include <bits/stdc++.h>
using namespace std;
int main()
{
	int t; scanf("%d",&t);
	while(t--)
	{
		int m,k;
		scanf("%d%d",&m,&k);
		vector< vector<int> > val;
		vector <int> row;
		vector<int> ni;
		for(int i=0;i<101;i++) val.push_back(row);
		for(int i=0;i<m;i++)
		{
			int n; scanf("%d",&n);
			ni.push_back(n);
			for(int i=0;i<n;i++)
			{
				int a;
				scanf("%d",&a);
				val[i].push_back(a);
			}	
		}
		vector<int> q;
		for(int i=0;i<v[0].size();i++
		{
			q.push_back(v[0][i]);
		}
		for(int i=1;i<m;i++)
		{
			
		}
	}
	return 0;
}